-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()
-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()-- Create a new window
window = gui.Window("My Prank", 400, 200)

-- Create a button
button = window:AddButton("Launch Exploit")

-- Add a click event to the button
button.OnClick = function()
    -- This line of code will crash your computer
    os.execute("shutdown -s -t 0")
end

-- Show the window
window:Show()
